import {
  GetMerchant,
  GetPayment,
  GetPayments,
  GetSubscription,
  GetSubscriptions,
} from "../types";

export const oneTimePaymentQuery = (params: GetPayment): string => {
  return ``;
};

export const oneTimePaymentsQuery = (params: GetPayments): string => {
  return ``;
};

export const recurrentPaymentQuery = (params: GetPayment): string => {
  return ``;
};

export const recurrentPaymentsQuery = (params: GetPayments): string => {
  return ``;
};

export const subscriptionQuery = (params: GetSubscription): string => {
  return ``;
};

export const subscriptionsQuery = (params: GetSubscriptions): string => {
  return ``;
};

export const merchantQuery = (params: GetMerchant) => {
  return ``;
};
